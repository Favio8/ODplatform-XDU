# ODPlatform Web Frontend

## Run

```bash
cd apps/web-frontend
npm install
npm run dev
```

默认读取 `http://127.0.0.1:8000/api`。

如需改后端地址：

```bash
$env:VITE_API_BASE="http://127.0.0.1:8000/api"
npm run dev
```
